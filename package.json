{
  "name": "msar",
  "version": "0.3.5",
  "type": "module",
  "main": "./client/dist/index.js",
  "types": "./client/dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./client/dist/index.d.ts",
      "import": "./client/dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && vitest",
    "patch": "tsc && npm version --no-git-tag-version patch && git add .",
    "minor": "tsc && npm version --no-git-tag-version minor && git add .",
    "major": "tsc && npm version --no-git-tag-version major && git add ."
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
